# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Aero runtime data — NEVER commit the vault or backups (contains personal memory)
/data/
*.vault
*.vault-wal
*.vault-shm
*.aero-backup
/models/

# Node / UI
node_modules/
ui/dist/
# Rust / Tauri build artifacts (Control App)
ui/control-app/src-tauri/target/
ui/control-app/src-tauri/gen/
ui/control-app/dist/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Personal voice recordings — never commit (biometric/personal data).
# The recording protocol (README + manifest) is kept; the audio is not.
/rec/
*.m4a
*.wav
spikes/s3_testset/*.wav
.venv-nemo/
.venv-litellm/
