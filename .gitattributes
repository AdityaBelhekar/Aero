# Normalize line endings so the repo is consistent across machines.
* text=auto eol=lf
*.py text eol=lf
*.md text eol=lf
*.toml text eol=lf

# Binary / runtime artifacts (never diffed)
*.vault binary
*.aero-backup binary
